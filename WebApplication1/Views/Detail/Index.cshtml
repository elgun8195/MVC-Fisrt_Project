@using First_Application.Models
@model Detail
<div class="container">
            <div class="row ">
                <div class="card" style="width: 18rem;">
                    
                    <div class="card-body">
                        <h5 class="card-title">@Model.Model</h5>
                        <p class="card-text">@Model.Year</p>
                        <p class="card-text">@Model.Motor</p>
                       
                    </div>
                </div>
            </div>
        </div>